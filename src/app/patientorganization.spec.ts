import { Patientorganization } from './patientorganization';

describe('Patientorganization', () => {
  it('should create an instance', () => {
    expect(new Patientorganization()).toBeTruthy();
  });
});
